# 玄学Agent · 01_PRD需求文档（MVP v0.1）

> 对标：某厂杭州 AI应用全栈 35-65K JD。前端定死 Next.js。
> 目标：2周可演示、上线可迭代的八字命理MVP。不做算法训练，不做命理专家系统，只做“算准+解好+存档”。

## 1. 产品定位

从0到1的玄学C端产品：八字分析、大运流年、姓名分析、择日建议（MVP只做八字）、情绪陪伴与人生决策参考。
一句话：**排盘要准（规则算），解盘要稳（AI解+引用约束），档案可回看（长期存），后台可调（人设/Prompt/知识）。**

## 2. 用户与场景

* 用户：对八字/运势感兴趣的C端，泛心理+泛娱乐+决策参考，非专业命理师。
* 场景：首次录入出生信息→看命盘→看AI结构化分析→追问事业/感情/财运→存档回看→分享（MVP不做分享）。
* 口径红线：陪伴/参考/传统文化视角，不做确定性断言（不写“必发财/必离婚”），未成年人引导至娱乐参考。

## 3. MVP范围（对标JD 8条，逐条可验）

| # | JD要求 | 本MVP实现 | 验收 |
| --- | --- | --- | --- |
| 1 | 输入出生年月日时、出生地、性别 | 录入页：日期+时间（未知时辰可选）+性别+出生地（省市）+真太阳时开关 | 缺时辰可算，缺地默认北京时间并提示 |
| 2 | 生成基础命盘 | 后端排盘API：四柱+藏干+五行+十神（首版）+大运（次版） | 与权威万年历抽10例全对 |
| 3 | AI结构化八字分析 | 分析页：命局总览+五行喜忌（规则给）+AI事业/感情/财运三段（JSON结构化） | 每段必引用命盘原文，不编造干支 |
| 4 | 追问 | 对话页：多轮+流式+上下文带命盘摘要 | 连续追问不丢命盘，不重算 |
| 5 | 保存命盘+历史 | 档案页：命盘+分析+对话按用户存，列表回看 | 刷新不丢，切用户隔离 |
| 6 | 前端美观高级感 | Next.js国风暖纸风，复用求职Agent sage体系 | 移动端优先，1680流体版心兼容 |
| 7 | 后台可配角色/Prompt/知识 | /admin页：人设+3模板+知识开关，改完即生效 | 不改代码可换话术 |
| 8 | 真实上线可迭代 | Docker+云服务器可跑，Vercel可发前端 | 一键启动脚本+health检查 |

## 4. 不做（v0.1明确）

* 不做姓名/择日/风水/占星塔罗；不做支付会员（预留字段）；不做分享海报；不做复杂多Agent；不做训练微调。

## 5. 功能清单（前端Next.js 5页）

* `/` 录入：表单+校验+真太阳时说明+“生成命盘”CTA
* `/chart` 命盘：四柱卡+五行条+十神表+大运时间线（占位可折叠）
* `/analysis` 分析：结构化三段+引用角标+“继续追问”跳对话
* `/chat` 对话：流式气泡+追问建议 chips+历史折叠
* `/archives` 档案：列表+详情回看
* `/admin` 后台：角色/Prompt/模型/知识开关/温度

## 6. 后端接口（MVP 6个）

* `POST /api/chart` {birth} → {pillars, hiddenStems, wuxing, shishen, dayun, solarTime} 纯规则，无LLM
* `POST /api/analyze` {chartId} → {overview, career, love, wealth, citations} LLM结构化
* `POST /api/chat` {chartId, messages} → 流式 tokens（SSE）
* `GET /api/archives` / `GET /api/archives/:id` 回看
* `GET/PUT /api/admin/config` 后台配置
* `GET /api/health` 健康检查

## 7. 数据与合规

* 存：users(匿名id)/charts(出生+排盘JSON)/analyses/chat_messages/configs/knowledge_docs
* 合规：出生数据本地加密存，不上传第三方；分析页常驻“娱乐参考”声明；日志脱敏。
* 降级：无Key时返回规则版总览+占位话术，排盘与档案照常用。

## 8. 验收标准（演示5分钟）

1. 录入1990-05-01 08:30 杭州男→命盘秒出，与万年历一致。
2. 点分析→三段+引用齐全，无编造干支。
3. 追问“今年财运”→流式+不丢命盘。
4. 刷新档案还在，admin改一句人设即生效。
5. `health` OK，一键脚本拉起前后端。
