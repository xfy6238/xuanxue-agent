# 玄学Agent · 02_技术方案（MVP v0.1）

> 前端定死 Next.js。后端复用求职Agent proven栈：FastAPI+SQLite+LLM Responses API。排盘实测通过。

## 1. 架构

```
[Next.js Web 5页 / + /chart /analysis /chat /archives /admin]
        │ REST + SSE流式
        ▼
[FastAPI 网关 :8322] ─ sessions/charts SQLite（PG兼容schema，M2切Supabase）
        ├─→ [规则排盘 chart.py] lunar-python，无LLM，唯一真源
        ├─→ [AI解盘] Responses API 结构化JSON（overview/career/love/wealth+citations）
        │     无Key降级 rules模板
        └─→ [知识库 M2] 玄学docs → Chroma RAG（先开关占位，MVP默认关）
```

## 2. 排盘选型（已实测 2026-09-06）

* 选用 `lunar-python`（pip），`Solar.fromYmdHms→getLunar→getEightChar`。
* 实测 `1990-05-01 08:30 → 庚午 庚辰 丙寅 壬辰`，四柱直出，藏干/五行/十神由库派生，不让LLM算。
* 真太阳时：M1按经度 `+ (lon-120)*4min` 粗校，开关可关；时辰未知→只出三柱并提示。
* 校验集：10例万年历对照（见03评测），错1例即 блокиратор。

## 3. 数据表（SQLite首版，字段与PG/Supabase同名）

* charts(id, birth_json, pillars_json, solar_corr, created)
* analyses(chart_id, overview, career, love, wealth, citations_json, llm)
* messages(chart_id, role, content, created)
* configs(key, value_json) 后台人设/Prompt/模型/开关

## 4. 接口（6个，同PRD）

* POST /api/chart 纯规则 <50ms；POST /api/analyze LLM JSON；POST /api/chat SSE流式；GET archives；GET/PUT admin/config；GET /health

## 5. Prompt（3个，admin可改）

* analyze_system：你是命理陪伴师，只基于给定命盘JSON解说，禁止改干支；每段带引用[from:年柱庚午]；结尾必加“娱乐参考”；禁断言（必/一定会）。
* chat_system：带命盘摘要+历史，追问不重算，不编新柱。
* rules降级：五行计数直出+通用话术，保证无Key可演示。

## 6. 前端Next.js（App Router+TS+Tailwind）

* 复用求职Agent暖纸sage体系，国风卡片+移动优先。
* /chart命盘卡 /analysis三段+角标 /chat SSE气泡 /archives回看 /admin表单。
* 环境变量 NEXT_PUBLIC_API=<http://127.0.0.1:8322。>

## 7. 工程

* 端口：后端8322（避开求职8321），前端3000系。
* 一键：run_demo.sh → pip install → uvicorn 8322 → npm run dev → open浏览器。
* M2再切：Supabase PG+Auth、会员支付字段预留、Chroma知识库接入。
