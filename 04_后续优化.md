# 玄学Agent · 后续优化记录（2026-09-07）

## RAG引用稳定率（待定，暂不做）

- 现状：RAG链路已通（744切片/检索注入/模型化用+`[from:书名]`引用均实测通过）。
- 现象：引用出现率约50%——料每次都进prompt，模型有时不用（low-effort推理波动+“无关则不用”指令）。
- 根因定位：用料层（模型裁量），非给料层（检索/切片稳定）。
- 候选方案③（推荐）：固定“典籍说”引用位，单独成段，结构可控，杜绝偷懒。
- 备选：①硬约束至少引用一次（恐硬套）；②后校验无引用则重试（+30~60秒）。
- 前置缺口：穷通宝鉴“十干×十二月”详文暂缺公开源，补到后检索精度还有一涨。

## 其他已知缺口（M2）

- 登录/支付/分享海报、Supabase切换、按页title、深色模式（设计选择，保持浅色）。
